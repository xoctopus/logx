package internal

const (
	KEY_TIMESTAMP = "@ts"
	KEY_LEVEL     = "@lv"
	KEY_MESSAGE   = "@msg"
	KEY_SOURCE    = "@src"
)

const (
	// TIME_FORMAT timestamp format
	TIME_FORMAT = "20060102-150405.000"
	// MASKED used to mask sensitive content
	MASKED = "--masked--"
)

const (
	BadKey       = "!BADKEY"
	MissingValue = "!MISSINGVALUE"
)
